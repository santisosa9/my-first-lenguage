Program {

    int getMax(int x, int y) {
        if (x > y) then {
            return x;
        } else {
            return y;
        }
    }

    void print_int(int x) extern;

    void main() {
        int x = -5;
        print_int(x);
        int j = 6 * 2;
        int y = -(-j--1);
        bool b = true;
        bool c = !b;
        int max = getMax(6,9);
        print_int(max);
    }

}

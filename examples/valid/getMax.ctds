Program {

    int getMax(int x, int y) {
        if (x > y) then {
            return x;
        } else {
            return y;
        }
    }

    void print_int(int x) extern;

    void main() {
        int max = getMax(6,9);
        print_int(max);
    }

}
